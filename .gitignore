# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*

# Dependencies
node_modules
.pnpm-store

# Build output
dist
dist-ssr
*.local
*.tsbuildinfo

# Test / coverage
coverage

# Playwright: the mock-mode build it serves, and what it writes when it fails
dist-e2e
dist-demo
playwright-report
test-results

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example

# Vendored agent skills — content is restored from skills-lock.json
# (the lock IS tracked); these dirs are generated, not source.
.agents/skills/
.claude/skills/

*storybook.log
storybook-static
